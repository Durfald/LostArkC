using LostArkCommercial.LostArkBot;
using LostArkCommercial.LostArkBot.Other;
using Newtonsoft.Json;
using PuppeteerSharp;

await Parser.ParseAsync("flextir");
//var bot = new Bot();
//bot.RunAsync().GetAwaiter().GetResult();
//var options = new LaunchOptions() { Headless = true, DefaultViewport = new ViewPortOptions() { Width = 2000, Height = 5000 } };
//var browser = await Puppeteer.LaunchAsync(options);
//var page = await browser.NewPageAsync();
//await page.GoToAsync("https://lostarktree.ru/rating/Jouki", timeout: 0, waitUntil: new[] { WaitUntilNavigation.Networkidle2 });
//var q = await page.XPathAsync(Xpath.Fetr);
//var w = q[0];
//var box = await w.BoundingBoxAsync();
//await page.Mouse.MoveAsync(box.Width / 2 + box.X, box.Height / 2 + box.Y);
//var objects = await page.EvaluateExpressionAsync<object>(JSCode.GetFetr);
//Console.WriteLine(JsonConvert.SerializeObject(objects, Formatting.Indented));
//await browser.CloseAsync();
//browser.Dispose();


